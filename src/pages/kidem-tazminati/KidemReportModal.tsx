/**
 * Kıdem rapor modalı = Dönemsel haftalık ile birebir aynı.
 * Tek kaynak: @/components/report BaseReportModal (sürüklenebilir, 16cm, Yazdır/Word/PDF).
 */
export { default } from "@/components/report/BaseReportModal";
export type { ReportConfig } from "@/components/report";
